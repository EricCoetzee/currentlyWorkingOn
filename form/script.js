
  /* **************************************************************************************** */

function showPreviewOne(event){
  if(event.target.files.length > 0){
    let src = URL.createObjectURL(event.target.files[0]);
    let preview = document.getElementById("file-ip-1-preview");
    preview.src = src;
    preview.style.display = "block";
  } 
}
function myImgRemoveFunctionOne() {
    document.getElementById("file-ip-1-preview").src = "img/default.png";
  }
  /* **************************************************************************************** */

  function showPreviewTwo(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-2-preview");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionTwo() {
      document.getElementById("file-ip-2-preview").src = "img/default.png";
    }
  /* **************************************************************************************************** */
  function showPreviewThree(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-3-preview");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionThree() {
      document.getElementById("file-ip-3-preview").src = "img/default.png";
    }
  /* **************************************************************************************************** */
  function showPreviewFour(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-4-preview");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionFour() {
      document.getElementById("file-ip-4-preview").src = "img/default.png";
    }
    /* **************************************************************************************** */

    function showPreviewFive(event){
      if(event.target.files.length > 0){
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-ip-5-preview");
        preview.src = src;
        preview.style.display = "block";
      } 
    }
    function myImgRemoveFunctionFive() {
        document.getElementById("file-ip-5-preview").src = "img/default.png";
      }
    /* **************************************************************************************************** */
    function showPreviewSix(event){
      if(event.target.files.length > 0){
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-ip-6-preview");
        preview.src = src;
        preview.style.display = "block";
      } 
    }
    function myImgRemoveFunctionSix() {
        document.getElementById("file-ip-6-preview").src = "img/default.png";
      }
    /* **************************************************************************************************** */

























    
  /* **************************************************************************************** */

function showPreviewOneTwo(event){
  if(event.target.files.length > 0){
    let src = URL.createObjectURL(event.target.files[0]);
    let preview = document.getElementById("file-ip-1-previewTwo");
    preview.src = src;
    preview.style.display = "block";
  } 
}
function myImgRemoveFunctionOneTwo() {
    document.getElementById("file-ip-1-previewTwo").src = "img/default.png";
  }
  /* **************************************************************************************** */

  function showPreviewTwoTwo(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-2-previewTwo");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionTwoTwo() {
      document.getElementById("file-ip-2-previewTwo").src = "img/default.png";
    }
  /* **************************************************************************************************** */
  function showPreviewThreeTwo(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-3-previewTwo");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionThreeTwo() {
      document.getElementById("file-ip-3-previewTwo").src = "img/default.png";
    }
  /* **************************************************************************************************** */
  function showPreviewFourTwo(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-4-previewTwo");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionFourTwo() {
      document.getElementById("file-ip-4-previewTwo").src = "img/default.png";
    }
    /* **************************************************************************************** */

    function showPreviewFiveTwo(event){
      if(event.target.files.length > 0){
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-ip-5-previewTwo");
        preview.src = src;
        preview.style.display = "block";
      } 
    }
    function myImgRemoveFunctionFiveTwo() {
        document.getElementById("file-ip-5-previewTwo").src = "img/default.png";
      }
    /* **************************************************************************************************** */
    function showPreviewSixTwo(event){
      if(event.target.files.length > 0){
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-ip-6-previewTwo");
        preview.src = src;
        preview.style.display = "block";
      } 
    }
    function myImgRemoveFunctionSixTwo() {
        document.getElementById("file-ip-6-previewTwo").src = "img/default.png";
      }
    /* **************************************************************************************************** */
































    /* **************************************************************************************** */

function showPreviewOneThree(event){
  if(event.target.files.length > 0){
    let src = URL.createObjectURL(event.target.files[0]);
    let preview = document.getElementById("file-ip-1-previewThree");
    preview.src = src;
    preview.style.display = "block";
  } 
}
function myImgRemoveFunctionOneThree() {
    document.getElementById("file-ip-1-previewThree").src = "img/default.png";
  }
  /* **************************************************************************************** */

  function showPreviewTwoThree(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-2-previewThree");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionTwoThree() {
      document.getElementById("file-ip-2-previewThree").src = "img/default.png";
    }
  /* **************************************************************************************************** */
  function showPreviewThreeThree(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-3-previewThree");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionThreeThree() {
      document.getElementById("file-ip-3-previewThree").src = "img/default.png";
    }
  /* **************************************************************************************************** */
  function showPreviewFourThree(event){
    if(event.target.files.length > 0){
      let src = URL.createObjectURL(event.target.files[0]);
      let preview = document.getElementById("file-ip-4-previewThree");
      preview.src = src;
      preview.style.display = "block";
    } 
  }
  function myImgRemoveFunctionFourThree() {
      document.getElementById("file-ip-4-previewThree").src = "img/default.png";
    }
    /* **************************************************************************************** */

    function showPreviewFiveThree(event){
      if(event.target.files.length > 0){
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-ip-5-previewThree");
        preview.src = src;
        preview.style.display = "block";
      } 
    }
    function myImgRemoveFunctionFiveThree() {
        document.getElementById("file-ip-5-previewThree").src = "img/default.png";
      }
    /* **************************************************************************************************** */
    function showPreviewSixThree(event){
      if(event.target.files.length > 0){
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-ip-6-previewThree");
        preview.src = src;
        preview.style.display = "block";
      } 
    }
    function myImgRemoveFunctionSixThree() {
        document.getElementById("file-ip-6-previewThree").src = "img/default.png";
      }
    /* **************************************************************************************************** */